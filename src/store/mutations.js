export default {
    SET_USER(state, userInfo) {
        state.userInfo = userInfo;
    },
    SET_ITEM(state, item) {
        state.item = item;
    },
    SET_LIST(state, list) {
        state.list = list;
    }
}